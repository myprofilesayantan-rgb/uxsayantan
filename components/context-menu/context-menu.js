/**
 * Custom Context Menu Component
 * Disabled per user request - allows standard default browser right-click behavior.
 */
window.PortfolioContextMenu = {
  init() {
    // Custom context menu disabled.
  }
};
